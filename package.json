{
  "name": "velometeo",
  "private": true,
  "type": "module",
  "description": "Dev-only manifest: marks .js as ES modules so `node --test tests/` runs. The site itself has no build step and no dependencies.",
  "scripts": {
    "test": "node --test 'tests/*.test.js'"
  }
}
